{
  //В стандарте JSON символ "@" не имеет никакого специального значения.
  //Однако, в данном контексте, я использовал символ "@" для обозначения
  // атрибутов XML при их переводе в JSON формат.
  //
  //Обычно атрибуты в XML преобразуются в поля объекта JSON
  //с префиксом или как ключ-значение, где ключ содержит символы,
  //указывающие на их атрибутность. Это делается для того,
  //чтобы сохранить информацию об атрибутах XML при переводе в JSON.
  //В данном случае, я решил использовать символ "@" для ясности и
  //удобства обозначения атрибутов в JSON.
  "request": {
    "@id": "25",
    "start-row": {
      "method": "GET",
      "url": "www.google.com",
      "version": "1.1"
    },
    "headers": {
      "header": [
        {
          "name": "content-type",
          "value": "text/html"
        },
        {
          "name": "accept",
          "value": "application/json"
        }
      ]
    },
    "body": {
      "@type": "text/plain"
    }
  }
}
